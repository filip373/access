.card-panel.blue.lighten-5.hoverable
  .thumbnail
    .caption
      %h4
        What about generating permissions repo?
      Generate...
      %hr
      .buttons
        - with_options(class: 'btn btn-primary btn-lg') do |opts|
          = opts.link_to 'Generate users', generate_users_path
          = opts.link_to 'Generate github teams', github_generate_permissions_path
          = opts.link_to 'Generate google groups', google_generate_permissions_path
      .buttons
        - with_options(class: 'btn btn-primary btn-lg') do |opts|
          = opts.link_to 'Generate rollbar teams', rollbar_generate_permissions_path
          = opts.link_to 'Generate toggl teams', toggl_generate_permissions_path
