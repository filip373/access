%nav.navbar.navbar-default.navbar-fixed-top
  .container
    .navbar-header
      = link_to 'Access app', root_path, class: 'navbar-logo'
    .pull-right
      = link_to 'relogin google', google_oauth2_path if session[:credentials].present?
