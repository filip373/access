
deployment:
  master:
    branch: master
    commands:
      - bundle exec cap staging deploy
      - RAILS_ENV=staging rake notify
  production:
    branch: production
    commands:
      - bundle exec cap production deploy
      - RAILS_ENV=production rake notify
