
deployment:
  master:
    branch: master
    commands:
      - cap deploy staging
      - RAILS_ENV=staging rake notify
  production:
    branch: production
    commands:
      - cap deploy production
      - RAILS_ENV=production rake notify
